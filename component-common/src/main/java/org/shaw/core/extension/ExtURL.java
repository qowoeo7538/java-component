package org.shaw.core.extension;

import org.shaw.util.CollectionHelper;

import java.io.Serializable;
import java.util.Collections;
import java.util.HashMap;
import java.util.Map;

/**
 * @create: 2018-06-01
 * @description:
 */
public final class ExtURL implements Serializable {

    /*private final Map<String, String> parameters;

    protected ExtURL() {
        this.parameters = null;
    }

    public ExtURL(String... pairs) {
        this(CollectionHelper.toStringMap(pairs));
    }

    public ExtURL(Map<String, String> parameters) {
        if (parameters == null) {
            parameters = new HashMap<>();
        } else {
            parameters = new HashMap<>(parameters);
        }
        this.parameters = Collections.unmodifiableMap(parameters);
    }

    public String getParameter(String key) {
        String value = parameters.get(key);
        if (String) {

        }
    }

    public String getParameter(String key, String defaultValue) {
        String value = getParameter(key);
    }*/
}
